<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - user</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <link rel="stylesheet" href="/users.css">
    <link rel="icon" href="/logo.png">
</head>
<body>
    <h2><i class="fas fa-users"></i> All Registered Users</h2>
    <p class="description">Below is the list of all users in the system. You can edit their usernames or delete accounts securely.</p>

    <form method="GET" action="/user/new">
        <button><i class="fas fa-user-plus"></i>Add New User</button>
    </form>
    
    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>User Name</th>
                    <th>EmailID</th>
                    <th>Edit</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody>
                <% for(user of result) { %>
                    <tr>
                        <td data-label="ID"><%= user.id %></td>
                        <td data-label="User Name"><%= user.username %></td>
                        <td data-label="EmailID"><%= user.email %></td>
                        <td data-label="Edit">
                            <form action="/user/<%= user.id %>/edit" method="get">
                                <button><i class="fas fa-edit"></i> Edit</button>
                            </form>
                        </td>
                        <td data-label="Delete">
                            <form method="get" action="/user/<%= user.id %>/delete">
                                <button><i class="fas fa-trash"></i> Delete</button>
                            </form>
                        </td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
    <a href="http://localhost:8080/"><i class="fas fa-home"></i>Back to Home</a>
</body>
</html>